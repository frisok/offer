spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5433/offer
    username: offer
    password: 123offerabc
    driverClassName: org.postgresql.Driver

